// Copy this file to Secrets.xcconfig (git-ignored) and fill in the real key.
// Secrets.xcconfig is included optionally by Debug.xcconfig/Release.xcconfig,
// so a missing key does not break the build (GMSApiKey resolves to empty and
// AppDelegate.swift skips GMSServices.provideAPIKey when it's blank).
MAPS_API_KEY=
