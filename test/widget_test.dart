import 'package:flutter_test/flutter_test.dart';

// Smoke tests will be added per-feature.
// The default counter test was removed since this app uses a custom entry point.
void main() {
  test('placeholder', () => expect(true, isTrue));
}
